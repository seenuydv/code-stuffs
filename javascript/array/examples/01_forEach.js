// Using forEach Loop to print array data with
// normal calling function.
let data = ["User01", "User02", "User03", "User04"];

data.forEach(cool);

function cool(value, id){
  console.log("id: "+ id +" value is: "+value);
}
